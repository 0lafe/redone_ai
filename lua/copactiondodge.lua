-- Possibly useless now. Saving for future check. 

-- Hooks:PostHook(CopActionDodge, "init", "RDAI_init_copactiondodge", function(self)
--     if self._ext_anim.upper_body_active and not self._ext_anim.upper_body_empty then
--         self._ext_movement:play_redirect("up_idle")
--     end
-- end)